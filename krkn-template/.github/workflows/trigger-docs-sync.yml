name: Trigger docs sync

# Part of the krkn docs-sync bot: krkn-chaos/website#320.
#
#   Watches   containers/krknctl-input.json, which owns the global parameters
#   Sends     "globals" to Doc Sync on StrikerEureka34/website_2
#   Changes   nothing here: its app token is scoped to website_2
#
# push, not pull_request: a fork PR gets no secrets, and a merge is a push anyway.

on:
  push:
    branches: [main]
    paths:
      - containers/krknctl-input.json

permissions:
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Mint a website-scoped app token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.DOC_SYNC_BOT_APP_ID }}
          private-key: ${{ secrets.DOC_SYNC_BOT_APP_PRIVATE_KEY }}
          owner: StrikerEureka34
          repositories: website_2

      - name: Dispatch doc-sync on the website
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          # One file, one target: "globals" routes to the bot's global entry
          # point rather than to a scenario directory.
          gh workflow run doc-sync.lock.yml \
            --repo StrikerEureka34/website_2 \
            -f scenarios="globals"
